# @skillsforllms/auth-setup

Authentication and authorization conventions for LLM agents.

```bash
npm install -D @skillsforllms/auth-setup skillsforllms
npx skillsforllms sync @skillsforllms/auth-setup
```
