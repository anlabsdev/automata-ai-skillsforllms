# @skillsforllms/react-setup

React + Vite + TypeScript project setup conventions for LLM agents.

```bash
npm install -D @skillsforllms/react-setup skillsforllms
npx skillsforllms sync @skillsforllms/react-setup
```
