# @skillsforllms/firebase-setup

Firebase setup conventions for Auth, Firestore, Storage, Functions, and rules.

```bash
npm install -D @skillsforllms/firebase-setup skillsforllms
npx skillsforllms sync @skillsforllms/firebase-setup
```
