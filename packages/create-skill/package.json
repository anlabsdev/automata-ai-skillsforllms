{
  "name": "create-skill",
  "version": "0.1.0",
  "description": "Scaffold a SkillsForLLMs skill package.",
  "type": "module",
  "license": "MIT",
  "bin": {
    "create-skill": "./dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@skillsforllms/core": "workspace:*",
    "fs-extra": "^11.2.0",
    "picocolors": "^1.1.1"
  }
}
